'use client';

import Payments from "../pages/Payments";

export default function PaymentsPage() {
  // We removed <Layout> because Next.js automatically wraps this 
  // in the layout.tsx found in your studio-new folder.
  return <Payments />;
}