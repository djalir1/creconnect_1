'use client';

import CalendarView from "../pages/CalendarView";

export default function Page() {
  // We removed <Layout> because the root layout.tsx already provides it.
  return <CalendarView />;
}